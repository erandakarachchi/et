import React from "react";

type Props = {};

const SideBar = (props: Props) => {
  return (
    <aside className="w-0 overflow-hidden md:w-64 bg-background border-r border-border h-full fixed top-[69px] left-0 ">
      SideBar
    </aside>
  );
};

export default SideBar;
